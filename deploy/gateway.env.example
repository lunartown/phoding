PORT=3002 # recommend 3002 to match frontend fallback
PUBLIC_GATEWAY_URL=https://api.example.com
# Optional: If you expose Vite separately (not recommended for MVP)
# PUBLIC_VITE_URL=https://preview.example.com

# Required
ANTHROPIC_API_KEY=

# Database (PostgreSQL)
# For local dev: postgresql://user:password@localhost:5432/phoding
# For GCP Cloud SQL: postgresql://user:password@/phoding?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
DATABASE_URL=postgresql://user:password@localhost:5432/phoding
